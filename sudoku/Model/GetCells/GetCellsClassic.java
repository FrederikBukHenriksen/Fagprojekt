package sudoku.Model.GetCells;

import java.util.ArrayList;
import java.awt.Point;

public class GetCellsClassic {

    public ArrayList<Point> getRowPeers(Point point, int[][] sudoku, int n, int k) {
        ArrayList<Point> peers = new ArrayList<>();
        for (int i = 0; i < sudoku.length; i++) {
            // peers.add(sudoku[point.x][i]);
        }
        return null;

    }
}
